---
- name: Restart calendar service
  systemd:
    name: calendar
    state: restarted
    enabled: yes
    daemon_reload: yes

- name: Reload nginx service
  systemd:
    name: nginx
    state: reloaded
    enabled: yes
