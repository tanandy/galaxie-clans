---
#
# Based on https://github.com/jitsi/docker-jitsi-meet
#
#- import_tasks: "checks.yml"
#  when:
#    - not _nginx_uninstall
#    - nginx_enable | bool

- include_tasks: "install.yml"
  when:
    - not _nginx_uninstall
    - nginx_enable | bool

- include_tasks: "uninstall.yml"
  when:
    - _nginx_uninstall
    - not (nginx_enable | bool)

- meta: flush_handlers