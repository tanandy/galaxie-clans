---
- name: Gathering facts
  hosts: "clans:!allies"
  become: yes
  gather_facts: yes
  tags:
    - always

- name: Deploy system-level services
  hosts: "{{ scope }}"
  become: yes
  gather_facts: no
  roles:
    - role: "system_base"
    - role: "system_users"
    - role: "backup"
    - role: "monitor"
    - role: "firewall"
    - role: "dns"
    - role: "mailserver"
