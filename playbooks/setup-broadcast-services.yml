---
- name: Setup broadcast services
  hosts: "{{ scope }}"
  become: yes
  gather_facts: yes

  roles:
    - role: "nginx"
      vars:
        nginx_skip_enabling_sites: yes

    - role: "container"
    - role: "jitsi-meet"
    - role: "mattermost"

    - role: "nginx"